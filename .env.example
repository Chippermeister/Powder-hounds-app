# Copy to .env.local (git-ignored) for local dev. The build bakes it into the page, so it isn't secret:
# restrict it to our domains in the ArcGIS dashboard instead.
# Esri World Imagery for the Satellite style outside the US. Without it, only the US has imagery.
VITE_ESRI_API_KEY=
